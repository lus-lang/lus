global pledge, require, coroutine

pledge("load", "fs:read")
local server = require("lus-server.main")
pledge("seal")

local simple_server = server.new()
simple_server:listen(8080)

while coroutine.pending() > 0 do
    coroutine.poll(5)
end